<h1>つぶやき一覧（index.html.erb）</h1>
<table>
    <tr>
        <td>コンテンツ</td>
    </tr>
    <% @posts.each do |post| %>
        <tr>
            <td><%= post.content %></td>
        </tr>
    <% end %>
</table>
